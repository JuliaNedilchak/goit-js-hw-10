const radioFul = document.querySelector('.fulfilled');
const radioRej = document.querySelector('.rejected');
const inputDelay = document.querySelector('input[name="delay"]');
const button = document.querySelector('button');
